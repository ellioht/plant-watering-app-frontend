import React from "react";

const UserId = (props) => {
  const { userId } = props;

  // use the userId prop as needed

  return null; // or any other JSX you want to return
};

export default UserId;